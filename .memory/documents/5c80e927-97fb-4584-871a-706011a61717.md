query-files now prints the full document body for each direct match. A hint line (`Use memorybank read <id> ...`) appears at the bottom of all query output. Added a README.md with quick start and reference.