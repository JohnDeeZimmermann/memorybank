add rejects body > 10,000 characters (chars, not bytes). query-files truncates body display to 2,000 characters with a per-document hint to use `memorybank read <id>`. Tests cover unicode counting, boundary cases, mixed truncation, and rebuild preservation.